export enum UserRoles {
  USER = 'USER',
  ADMIN = 'ADMIN',
}

export enum OrderStatus {
  PENDING = 'PENDING',
  PAID = 'PAID',
}

export enum ResourceType {
  CATEGORIES = 'categories',
  ORDERS = 'orders',
  PRODUCTS = 'products',
  USERS = 'users',
}
